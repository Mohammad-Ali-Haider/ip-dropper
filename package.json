{
  "name": "ip-dropper",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cross-env NODE_ENV=production concurrently \"npm run start -w frontend\" \"npm run start -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "ping": "^0.4.4"
  }
}
